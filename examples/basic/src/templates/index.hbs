<!doctype html>
<html {{{ helmet.html }}}>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
      {{{ helmet.meta }}}
      {{{ helmet.title }}}
      {{{ helmet.link }}}
  </head>
  <body {{{ helmet.body }}}>
  <div id="root">{{{ body }}}</div>
    {{{ append }}}
  </body>
</html>
